1. 在models.py中新建了userInfo类，对应数据库中存在一个名为userInfo的表格。
2. settings.py中找到DATABASE，修改用户名，密码为自己的mysql用户名和密码，数据库名字为powerdeskai。
3. 在你的编译器控制台上先后输入：pip install mysqlclient，Python manage.py makemigrations，Python manage.py migrate。
4. mysql相关操作的代码（在views.py中）：
    	1. 新建数据：
	    • 类名.objects.create(列名 = "数据"，列名 = "数据"，列名 = "数据"，…)
	    2. 删除数据：
	    • 类名.objects.filter(条件如id=3).delete()
	    • 类名.objects.all().delete()
	    3. 获取数据：
	    • 获取到的每一列数据都是一个对象，需要被赋值如：
	    • data_list = 类名.objects.all()
	      For obj in data_list:
		    Print(obj.id, obj.name, obj.age, …)
	    • Data_list = 类名.objects.filter(条件)
	    • 如果只需要第一条或第二条数据：row_obj = 类名.objects.all().first()
	    4. 更新数据：
	    • 类名.objects.filter(条件如id=3).update(条件)
        • 类名.objects.all().update(条件)